name: Deploy Backend to Azure Container Apps

on:
  push:
    paths:
      - backend/**
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Azure ACR Login
        run: |
          az acr login --name faircr

      - name: Build and Push Docker image for linux/amd64
        run: |
          docker buildx build \
            --platform linux/amd64 \
            -t faircr.azurecr.io/fair-ai-backend:latest \
            --push ./backend

      - name: Deploy to Azure Container Apps
        run: |
          az containerapp update \
            --name fair-ai-backend \
            --resource-group fair-rg \
            --image faircr.azurecr.io/fair-ai-backend:latest

      - name: Set environment variables on Azure Container App
        run: |
          az containerapp update \
            --name fair-ai-backend \
            --resource-group fair-rg \
            --set-env-vars \
              =${{ secrets. }} \
              AZURE_OPENAI_ENDPOINT=${{secrets.AZURE_OPENAI_ENDPOINT}} \ 
              AZURE_OPENAI_API_KEY=${{secrets.AZURE_OPENAI_API_KEY}} \
              DATABASE_URL=${{ secrets.DATABASE_URL }} \
              FRONTEND_URL=${{ secrets.FRONTEND_URL }}
              